import 'package:flutter/material.dart';

class EarningTab extends StatelessWidget {
  const EarningTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Earning Tab"),
    );
  }
}
